/**
 * Owns: ArchUnit tests that enforce CORTEX layering and coding rules.
 * Never imports: production beans (it scans bytecode, not Spring context).
 * Owner: CORTEX :: log-gateway.
 */
package io.cortex.gateway.architecture;
