/**
 * Owns: servlet filters executed before any controller (correlation, security pre-checks).
 * Never imports: services, repositories, mappers.
 * Owner: CORTEX :: log-gateway.
 */
package io.cortex.gateway.filter;
