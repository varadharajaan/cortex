/**
 * Owns: thin HTTP controllers; translate request DTOs to service calls and back.
 * Never imports: repositories, JPA entities, persistence framework.
 * Owner: CORTEX :: log-gateway.
 */
package io.cortex.gateway.controller;
