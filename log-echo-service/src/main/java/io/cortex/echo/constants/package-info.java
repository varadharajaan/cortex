/**
 * Constants for log-echo-service: HTTP header names and MDC field
 * names. These are tiny holders so the controller, filters and tests
 * all reference the same literals (rule A6.5, A7.1).
 */
package io.cortex.echo.constants;
