/**
 * Owns: REST controllers that expose echo endpoints to verify gateway
 * proxy and discovery behavior in smoke tests.
 * Never imports: persistence, gateway internals, other Cortex services.
 * Owner: CORTEX :: log-echo-service.
 */
package io.cortex.echo.controller;
