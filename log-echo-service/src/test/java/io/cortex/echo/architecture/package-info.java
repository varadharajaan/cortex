/**
 * Owns: ArchUnit rules enforcing CORTEX layering, naming, and coding
 * rules at build time for log-echo-service.
 * Never imports: production code at runtime (test-scope only).
 * Owner: CORTEX :: log-echo-service.
 */
package io.cortex.echo.architecture;
