<?xml version="1.0" encoding="UTF-8"?>
<!--
  CORTEX log-processor-service Logback configuration (rule A8.1).
  - Default profile: structured JSON to stdout for container log collection.
  - Dev profile: human-readable console output (overrides the JSON appender).
  - MDC fields traceId / tenantId / userId are exported on every line (rule 17.5).
-->
<configuration>

    <property name="LOG_PATTERN_DEV"
              value="%d{yyyy-MM-dd'T'HH:mm:ss.SSSXXX} %-5level [%thread] %X{traceId:-} %logger{36} - %msg%n"/>

    <springProfile name="dev">
        <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
            <encoder>
                <pattern>${LOG_PATTERN_DEV}</pattern>
            </encoder>
        </appender>
        <root level="INFO">
            <appender-ref ref="STDOUT"/>
        </root>
    </springProfile>

    <springProfile name="!dev">
        <appender name="JSON" class="ch.qos.logback.core.ConsoleAppender">
            <encoder class="net.logstash.logback.encoder.LogstashEncoder">
                <includeMdcKeyName>traceId</includeMdcKeyName>
                <includeMdcKeyName>tenantId</includeMdcKeyName>
                <includeMdcKeyName>userId</includeMdcKeyName>
                <customFields>{"service":"log-processor-service"}</customFields>
                <timeZone>UTC</timeZone>
            </encoder>
        </appender>
        <root level="INFO">
            <appender-ref ref="JSON"/>
        </root>
    </springProfile>

</configuration>
