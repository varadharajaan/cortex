/**
 * Owns: package-private implementation of the SDK (HTTP transport, JSON codec, buffer).
 * Never imports: io.cortex.agent.logback.*, org.springframework.*.
 * Owner: CORTEX :: log-agent-lib (no public binary-compatibility guarantee).
 */
package io.cortex.agent.internal;
