/**
 * Inbound HTTP controllers (REST adapter layer). Translate HTTP
 * requests into service-layer calls and back into HTTP responses;
 * no business logic lives here (rule 8.1 / 8.2).
 */
package io.cortex.ingest.controller;
