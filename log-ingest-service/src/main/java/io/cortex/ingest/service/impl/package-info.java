/**
 * Implementations of the service-layer ports declared in
 * {@link io.cortex.ingest.service}. Annotated with Spring stereotypes
 * so they are picked up by component scan.
 */
package io.cortex.ingest.service.impl;
